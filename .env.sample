PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=
PG_USERNAME=
PG_PASSWORD=
PG_SSL_MODE=prefer   # disable, allow, prefer, require, verify-ca, verify-full

# Stored procedure to execute
SP_NAME=your_stored_procedure

# Optional: comma‑separated arguments for the stored procedure
# SP_ARGS=arg1,arg2
